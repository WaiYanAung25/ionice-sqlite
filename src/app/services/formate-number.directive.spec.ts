import { FormateNumberDirective } from './formate-number.directive';

describe('FormateNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new FormateNumberDirective();
    expect(directive).toBeTruthy();
  });
});
